forge-it:
  modules:
    postgresql:
      enabled: true
      mode: internal
      host: localhost
      port: 5432
      database: forge-it
      username: forge-it
      password: forge-it-pwd
      image: postgres:16-alpine
      template: postgresql-template
