forge-it:
  modules:
    wiremock:
      enabled: true
      mode: internal
      host: localhost
      port: 8089
      mapping:
        request: /mappings/requests
        response: /mappings/responses
