forge-it:
  modules:
    wiremock:
      enabled: true
      mode: internal
      host: localhost
      port: 8089
      mapping:
        request: /mappings/request
        response: /mappings/response
        default-request: /mappings/default/request
        default-response: /mappings/default/response

