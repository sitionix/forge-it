# HTTP E2E implementation plan

**Goal:** Execute the existing MockMvc DSL against typed external services.
**Architecture:** Keep fixture/default/mutation and portable assertion code in MockMvcBuilder. Pass plain request/response records through a transport executor; only the MVC adapter handles servlet requests and matchers. Bootstrap E2E with a plain Spring context and the existing feature installers and generated implementation.
**Spec:** /home/vlad/Downloads/forge_it_e2e_implementation_task.md
**BASE_SHA:** e339f2471a9e3fd21163c4b768d766430041c2b0

Constraints: no PR, merge, release, version changes, new DSL, external environment management or global mode/base URL. Branch feature/SITIONIX-66.

- [x] Characterize existing defaults, overrides, token suppression, matchers and assertion failures using standalone MockMvc before refactoring. Run `mvn -B -pl forge-it-mockmvc -am test`.
- [x] Add immutable domain ServiceContract and builder; preserve `${property}` definitions and validate at binding against the current Environment. Test missing, blank, unresolved and invalid addresses and independent contexts.
- [x] Extract MockMvcExecutor plus plain request/response records. Keep the old constructor for compatibility. Share assertions with JsonComparator; keep ResultMatcher exclusively in MVC adapter. Exercise both transports with the same endpoints and fixture loader.
- [x] Add a context-owned HTTP client with positive bounded connect/request timeouts, redirect NEVER, UTF-8 bodies and URI component encoding. Test local port-0 servers, 4xx/5xx, redirect, timeout, interrupt and binding isolation.
- [x] Add @E2E using SpringExtension/plain context. Reuse factory contract/feature discovery, installer and module defaults. Register only JSON infrastructure and the selected generated implementation. Reject unsupported features and mixed modes before installation.
- [x] Add generated consumer E2eSupport, property-based ServiceContracts and Docker-free self-tests reusing auth endpoints/fixtures. Document explicit live execution and reproducible self-test commands.
- [x] Run changed module tests and relevant consumer IT, audit diff and report commands/results/limitations and final HEAD.
